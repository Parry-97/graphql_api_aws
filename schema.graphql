type Hotel {
  id: ID
  short_description: String
  city: String
  average: Float
  number_of_votes: Int
  long_description: String
  hotel_name: String
}

type Query {
  getHotelById(id: ID!): Hotel
}

schema {
  query: Query
}
